{
    "name": "posts",
    "main": "./dist/index.mjs",
    "scripts": {
        "dev": "tsup ./index.ts --format esm --watch",
        "build": "tsup ./index.ts --format esm"
    },
    "devDependencies": {
        "front-matter": "^4.0.2",
        "tsup": "^6.4.0"
    }
}
